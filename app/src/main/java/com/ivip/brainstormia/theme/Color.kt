package com.ivip.brainstormia.theme

import androidx.compose.ui.graphics.Color

// Primary colors
val PrimaryColor = Color(0xFF1A5F7A) // Azul escuro principal
val PrimaryVariantColor = Color(0xFF3700B3)
val SecondaryColor = Color(0xFF2E7D9E) // Azul médio para elementos secundários no tema claro
val SecondaryColorDark = Color(0xFF212121) // Cor secundária para tema escuro
val SecondaryVariantColor = Color(0xFF018786)
val BrainGold = Color(0xFFFFD700)

// Background colors
val BackgroundColor = Color(0xFFF0F4F7) // Fundo claro com tom azulado
val BackgroundColorDark = Color(0xFF0F2A38) // Original: fundo escuro azulado
val BackgroundColorBlack = Color(0xFF121212) // Novo: cor de fundo preta para tema escuro

// Surface colors
val SurfaceColor = Color(0xFFFFFFFF) // Mantendo o branco para superfícies
val SurfaceColorDark = Color(0xFF1A3A4A) // Original: Superfície escura azulada
val SurfaceColorBlack = Color(0xFF212121) // Novo: superfície preta para tema escuro

// Input colors
val InputColorDark = Color(0xFF828282) // Cor #828282 para inputs no tema escuro

// Text colors
val TextColorLight = Color(0xFFF7F7F7) // Mantendo o branco para texto em fundos escuros
val TextColorDark = Color(0xFF121212) // Preto mais puro para texto

// TopBar colors
val TopBarColorDark = Color(0xFF1E1E1E) // Atualizar de Color.Black para #1E1E1E

// Message bubble colors
val UserBubbleColor = Color(0xFFD4E6F1) // Bolhas de mensagem do usuário em azul claro
val BotBubbleColor = Color(0xFF1A5F7A) // Bolhas do bot com a cor primária azul escuro